# CHANGELOG

## [v2.1.20260722191426-pure] - 2026-07-25

### Fixed & Security
- 🛡️ **安全脱敏与隐私保护**：彻底查杀并解出全部动态加密混淆（如 `String.fromCharCode` / 算术混淆），全盘删除原版的第三方数据埋点与隐蔽追踪。
- 📦 **纯本地 HTML 闭环**：所有前端页面（包含 Login、Admin、noKV、noADMIN）全量内嵌至单文件 `_worker.js` 内存中，移除外部 GitHub Pages 托管依赖，解决重定向死循环及 522 响应超时。
- 🐛 **修复 `/sub` 订阅路由崩溃**：补全 `生成随机IP()` 内部缺失的 `cfname` 变量声明，修复因 `ReferenceError` 导致节点数据无法拉取的底层致命 Bug。
- 🌐 **前端跨域（CORS）与 API 修复**：修补后台管理面板“国外测试”与“CloudFlare ProxyIP”测试卡片跨域被拦截问题，更新数据源为 `ipwho.is` 与 Worker 内置 `/admin/cf.json` 接口。
- 🗺️ **修复 ProxyIP 模态框**：修正开源 ProxyIP 数据源 `zip.cm.edu.kg/all.json` 关联解析，重写单 IP 验证超时规则，解锁全球大洲及国家代理选择。
- 🔒 **移除上游覆盖工作流**：删除 `.github/workflows/sync.yml`，防止自动同步任务复写覆盖纯净版代码。

---

## 上游历史版本基准 (Upstream Baseline)

### [2.1.20260722191426] - 2026-07-22
- 适配 `http://cp.cloudflare.com/generate_204` 真连接测试地址。

### [2.1.20260711190235] - 2026-07-11
- 新增 `TCP_CONCURRENT_DIAL` 及 `PROXY_CONCURRENT_DIAL` 环境变量，自定义拨号并发数。
- 优化反代及 DoH 缓存机制。
- 新增 Trojan 回退代理支持。
